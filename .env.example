# Copy to .env and fill in.
ROBO_EMAIL=you@example.com
ROBO_PASSWORD=changeme
ROBO_DEVICE_NAME=Living Room Bot
ROBO_DUID=

# Default map coordinate center (mm) and dance radius
DEFAULT_CENTER_X=32000
DEFAULT_CENTER_Y=27000
DEFAULT_RADIUS=800

# Roborock Vacuum Ballet - control/env example
# Copy to .env.ballet or .envs/controls.env and adjust values.

# Credentials (keep in base .env ideally)
# ROBO_EMAIL=your_email@example.com
# ROBO_PASSWORD=your_password_here

# Dance sizing and timing
DEFAULT_RADIUS=400
DEFAULT_BEAT_MS=1000
MIN_DANCE_RADIUS_MM=200
MAX_DANCE_RADIUS_MM=1200

# Arrival gating
ARRIVAL_THRESHOLD_MM=250
WAYPOINT_TIMEOUT_S=35
ARRIVAL_POLL_S=0.5
ARRIVAL_SETTLE_S=0.2

# Dock offset and preflight
DOCK_BUFFER_MM=300
ENABLE_GOTO_PREFLIGHT=1
PREFLIGHT_START_DELAY_S=0.4
PREFLIGHT_PAUSE_DELAY_S=0.3

# Fallback center
DEFAULT_CENTER_X=0
DEFAULT_CENTER_Y=0

# Map snapshot defaults
MAPWATCH_INTERVAL_S=5
MAPWATCH_COUNT=12

# Enable arrival gating
ENABLE_ARRIVAL_GATING=0

# Dance timing
DEFAULT_BEAT_MS=200
PREFLIGHT_START_DELAY_S=0.2
PREFLIGHT_PAUSE_DELAY_S=0.1